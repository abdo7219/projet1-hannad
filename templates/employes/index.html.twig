{% extends 'base.html.twig' %}

{% block title %}Hello EmployerController!
{% endblock %}

{% block body %}
<div class="container mt-3">
<table class="table" >
  <h1 class="titre text-center solid 1.2em ">Annuaire employés</h1>
  
   <thead class="thead-light">
    <tr>
      <th scope="col">#</th>
      <th scope="col">Prenom</th>
      <th scope="col">Nom</th>
      <th scope="col">Telephone</th>
      <th scope="col">Email</th>
      <th scope="col">Adresse</th>
      <th scope="col">salaire</th>
      <th scope="col">DateDeNAISSANCE</th>
      <th scope="col">Option</th>
    </tr>
  </thead>
  <tbody>
  {% for employer in tabEmployes %}
    <tr>
      <th scope="row">{{ employer.id }}</th>
      <td>{{ employer.prenom }}</td>
      <td>{{ employer.nom }}</td>
      <td>{{ employer.telephone }}</td>
      <td>{{ employer.email }}</td>
      <td>{{ employer.adresse }}</td>
      <td>{{ employer.salaire }}</td>
      <td>{{ employer.datedenaissance|date("d-m-Y" ) }}</td>
<td>
   <th>
      <a href="{{ path('employes_edit', {id:employer.id} ) }}"><i class="fa-solid fa-pen-to-square"></i></a>
      </th>

      <th>
          <a href="{{ path('employes_delete', {id:employer.id} ) }}"><i class="fa-solid fa-trash-can"></i></a>
          </th>
    </td>
    </tr>

     {% endfor %}
   
  </tbody>
</table>
<p class="text-center">
<a href="{{ path('employes_create') }}" class="btn btn-primary btn.center">Ajouter un Employer</a>
</p>
</div>
{% endblock %}